// import db from './db';

// db.query(
//   'SELECT * FROM workspace WHERE workspace_idx=?',
//   workspaceIdx,
//   (err, result) => {
//     return err ? reject(err) : resolve(result[0]);
//   }
// );
